name: Ultimate AI Bot

on:
  # زمان‌بندی هوشمند - فقط ساعات پرمخاطب ایرانی
  schedule:
    - cron: '0 10,14,18,22 * * *'  # 10ص، 2بعد، 6عصر، 10شب
  workflow_dispatch:

jobs:
  ai-bot:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        pip install requests
        pip install python-dateutil
        
    - name: Run Ultimate AI Bot
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
      run: |
        python -c "
import requests
import random
import json
import re
from datetime import datetime, timedelta
from collections import Counter
import time

# ==================== تنظیمات ====================
TOKEN = '${{ secrets.BOT_TOKEN }}'
CHANNEL = '@hoshmasnoye'

# ==================== کلاس تحلیل‌گر ====================
class UltimateAIAnalyzer:
    \"\"\"تحلیل‌گر هوشمند با یادگیری عمیق\"\"\"
    
    def __init__(self):
        self.learned_patterns = []
        self.best_keywords = []
        self.viral_templates = []
        self.competitor_data = {}
        self.best_times = [10, 14, 18, 22]  # ساعات طلایی ایرانی
        
    def analyze_content(self, content):
        \"\"\"تحلیل محتوای رقبا\"\"\"
        analysis = {
            'keywords': self.extract_keywords(content),
            'sentiment': self.analyze_sentiment(content),
            'structure': self.analyze_structure(content),
            'virality_score': self.calculate_virality(content),
            'engagement_triggers': self.find_engagement_triggers(content)
        }
        return analysis
    
    def extract_keywords(self, text):
        \"\"\"استخراج کلمات کلیدی هوشمند\"\"\"
        words = re.findall(r'[\\w\\u0600-\\u06FF]+', text.lower())
        
        # فیلتر کلمات عمومی
        stop_words = {
            'و', 'در', 'با', 'از', 'به', 'برای', 'این', 'آن', 'را',
            'که', 'یا', 'هم', 'یک', 'دو', 'سه', 'چهار', 'پنج'
        }
        
        filtered = [w for w in words if w not in stop_words and len(w) > 2]
        return Counter(filtered).most_common(10)
    
    def analyze_sentiment(self, text):
        \"\"\"تحلیل احساسات متن\"\"\"
        positive_words = {'عالی', 'ممتاز', 'ویژه', 'خاص', 'طلا', 'ناب', 'برتر'}
        negative_words = {'ضعیف', 'بد', 'ناموفق', 'اشتباه', 'غلط'}
        question_words = {'چرا', 'چطور', 'چگونه', 'آیا', 'کدام'}
        
        score = 0
        text_lower = text.lower()
        
        for word in positive_words:
            if word in text_lower:
                score += 1
        for word in negative_words:
            if word in text_lower:
                score -= 1
        for word in question_words:
            if word in text_lower:
                score += 0.5  # سوال‌ها تعامل رو بالا می‌برن
        
        return 'positive' if score > 0 else 'negative' if score < 0 else 'neutral'
    
    def calculate_virality(self, content):
        \"\"\"محاسبه احتمال ویرال شدن\"\"\"
        score = 0.3  # پایه
        
        # عوامل ویرال شدن
        factors = {
            '؟': 0.2, '!': 0.15,
            '🔥': 0.3, '🎯': 0.25, '💡': 0.2, '🚀': 0.25,
            'رایگان': 0.2, 'آموزش': 0.15, 'درآمد': 0.25,
            'فوری': 0.15, 'مهم': 0.1, 'ویژه': 0.15
        }
        
        for factor, value in factors.items():
            if factor in content:
                score += value
        
        return min(score, 1.0)
    
    def find_engagement_triggers(self, content):
        \"\"\"یافتن محرک‌های تعامل\"\"\"
        triggers = {
            'question': '؟' in content,
            'emoji': bool(re.findall(r'[\\u2700-\\u27bf]|[\\ud83c]\\udf00-\\udfff]', content)),
            'call_to_action': any(word in content.lower() for word in ['کامنت', 'نظر', 'جواب', 'رای']),
            'urgency': any(word in content.lower() for word in ['فوری', 'فقط', 'محدود', 'آخرین']),
            'number_list': bool(re.search(r'\\d+\\s*[\\u0600-\\u06FF]', content))
        }
        return triggers

# ==================== کلاس تولید محتوا ====================
class GeniusContentGenerator:
    \"\"\"تولیدکننده محتوای ناب\"\"\"
    
    def __init__(self, analyzer):
        self.analyzer = analyzer
        self.content_types = [
            'educational', 'question', 'project', 
            'news', 'tips', 'controversial', 'success_story'
        ]
        
    def generate_smart_content(self, context=None):
        \"\"\"تولید محتوای هوشمند با توجه به زمینه\"\"\"
        now = datetime.now()
        hour = now.hour
        weekday = now.weekday()  # 0 = دوشنبه
        
        # انتخاب نوع محتوا بر اساس زمان
        if hour < 12:
            content_type = 'educational'  # صبح‌ها آموزشی
        elif hour < 17:
            content_type = 'tips'  # ظهر نکته
        elif hour < 21:
            content_type = 'question'  # عصر سوال
        else:
            content_type = 'project'  # شب پروژه
        
        # تولید محتوا
        if content_type == 'educational':
            return self.generate_educational_content()
        elif content_type == 'question':
            return self.generate_question_content()
        elif content_type == 'project':
            return self.generate_project_content()
        elif content_type == 'tips':
            return self.generate_tips_content()
        else:
            return self.generate_viral_content()
    
    def generate_educational_content(self):
        \"\"\"تولید محتوای آموزشی\"\"\"
        topics = [
            'یادگیری ماشین', 'پایتون پیشرفته', 'ChatGPT حرفه‌ای',
            'داده‌کاوی عملی', 'هوش مصنوعی در کسب‌وکار', 'اتوماسیون هوشمند'
        ]
        
        templates = [
            \"\"\"🎓 آموزش عملی: {موضوع}
            
📌 این ۳ مرحله رو دنبال کن:
۱️⃣ {مرحله1}
۲️⃣ {مرحله2}
۳️⃣ {مرحله3}

🔥 رایگان و کاربردی\"\"\",
            
            \"\"\"📚 راهنمای کامل {موضوع}
            
✅ مزایا: {مزایا}
✅ زمان مورد نیاز: {زمان}
✅ سطح: {سطح}

💡 برای شروع کافیه...\"\"\",
            
            \"\"\"🎯 {موضوع} در ۷ روز
            
روز ۱: {روز1}
روز ۲: {روز2}
روز ۳: {روز3}
...

آماده‌ای چالش رو شروع کنی؟\"\"\"
        ]
        
        topic = random.choice(topics)
        template = random.choice(templates)
        
        # داده‌های پویا
        data = {
            'موضوع': topic,
            'مرحله1': 'مبانی رو قوی یاد بگیر',
            'مرحله2': 'پروژه کوچک تمرینی بساز',
            'مرحله3': 'نمونه کار حرفه‌ای ایجاد کن',
            'مزایا': 'درآمد بالا، آینده روشن، تقاضای زیاد',
            'زمان': '۳۰ روز با روزی ۱ ساعت',
            'سطح': 'مبتدی تا پیشرفته',
            'روز1': 'آشنایی با مفاهیم پایه',
            'روز2': 'نصب ابزارهای لازم',
            'روز3': 'اولین پروژه عملی'
        }
        
        for key, value in data.items():
            template = template.replace('{' + key + '}', value)
        
        return template
    
    def generate_question_content(self):
        \"\"\"تولید محتوای سوالی\"\"\"
        questions = [
            \"به نظر شما هوش مصنوعی تا ۵ سال آینده چه تحولی ایجاد می‌کنه؟\",
            \"اگر امروز می‌خواستید برنامه‌نویسی رو شروع کنید، از کدام زبان شروع می‌کردید؟\",
            \"بهترین پروژه عملی برای رزومه یک برنامه‌نویس چیه؟\",
            \"ChatGPT چطور می‌تونه درآمدزایی ایجاد کنه؟\",
            \"مهم‌ترین مهارت برای موفقیت در حوزه AI چیه؟\"
        ]
        
        formats = [
            \"\"\"❓ سوال مهم امروز:
{سوال}

💭 نظر شما چیه؟
👇 کامنت کن تا بحث رو شروع کنیم!\"\"\",
            
            \"\"\"🤔 {سوال}

۱. گزینه اول
۲. گزینه دوم  
۳. گزینه سوم

👇 رای بدید و دلیلتون رو بنویسید!\"\"\",
            
            \"\"\"💬 بحث آزاد:
{سوال}

نظرات، تجربیات و ایده‌هاتون رو به اشتراک بذارید 🔥\"\"\"
        ]
        
        question = random.choice(questions)
        format = random.choice(formats)
        
        return format.replace('{سوال}', question)
    
    def generate_project_content(self):
        \"\"\"تولید محتوای پروژه‌محور\"\"\"
        projects = [
            ('ربات تلگرام هوشمند', 'پایتون + کتابخانه‌های رایگان', '۲ روز', '۵۰۰-۲M تومان'),
            ('سیستم توصیه‌گر فیلم', 'یادگیری ماشین', '۱ هفته', 'پورتفولیو قوی'),
            ('اپ موبایل مالی', 'React Native', '۲ هفته', '۳-۵M تومان'),
            ('اتوماسیون اداری', 'Python + Selenium', '۳ روز', 'صرفه‌جویی زمانی'),
            ('چت‌بات مشاوره', 'ChatGPT API', '۱ هفته', 'ارائه خدمات')
        ]
        
        project, tools, time, income = random.choice(projects)
        
        templates = [
            \"\"\"🚀 پروژه عملی: {پروژه}
            
🔧 ابزارها: {ابزارها}
⏱️ زمان: {زمان}
💰 درآمد: {درآمد}

🎯 آموزش قدم‌به‌قدم در کامنت‌ها 👇\"\"\",
            
            \"\"\"💼 پروژه درآمدزا: {پروژه}
            
✅ مهارت‌های مورد نیاز: پایتون، منطق برنامه‌نویسی
✅ پیش‌نیاز: علاقه به یادگیری
✅ نتیجه: {درآمد}

🔥 این فرصت رو از دست نده!\"\"\",
            
            \"\"\"🔨 ساخت {پروژه} از صفر
            
🛠️ {ابزارها}
📅 {زمان}
🏆 {درآمد}

آماده‌ای شروع کنیم؟ کامنت کن!\"\"\"
        ]
        
        template = random.choice(templates)
        template = template.replace('{پروژه}', project)
        template = template.replace('{ابزارها}', tools)
        template = template.replace('{زمان}', time)
        template = template.replace('{درآمد}', income)
        
        return template
    
    def generate_tips_content(self):
        \"\"\"تولید محتوای نکته و ترفند\"\"\"
        tips = [
            'با ChatGPT می‌تونی کدت رو ۵۰٪ سریع‌تر دیباگ کنی',
            'هر روز ۳۰ دقیقه کد بزن بهتر از هفته‌ای ۴ ساعت‌ست',
            'پروژه‌های کوچک ولی کامل بساز نه بزرگ و ناتمام',
            'مستندسازی رو جدی بگیر، آینده‌ت رو می‌سازه',
            'از Git استفاده کن، حتی برای پروژه‌های شخصی'
        ]
        
        tip = random.choice(tips)
        
        formats = [
            f\"💡 نکته طلایی امروز:\\n\\n{tip}\\n\\nاین تکنیک رو حتما امتحان کن!\",
            f\"🎯 ترفند حرفه‌ای:\\n\\n{tip}\\n\\nکاربردی و تست شده ✅\",
            f\"🌟 الماس محتوا:\\n\\n{tip}\\n\\nاینو از دست نده! 🔥\"
        ]
        
        return random.choice(formats)
    
    def generate_viral_content(self):
        \"\"\"تولید محتوای ویرال\"\"\"
        viral_templates = [
            \"\"\"🚨 مهم: {موضوع}
            
این رو هیچ‌کس بهت نمی‌گه!
👇 ادامه در کامنت‌ها...\"\"\",
            
            \"\"\"🔥 {موضوع} رو اشتباه یاد می‌گیری!
            
درستش اینه: {راه‌درست}
نظرات شوکه‌کننده در کامنت‌ها 👇\"\"\",
            
            \"\"\"❗ هشدار: {موضوع}
            
اگر اینکار رو نکنی، {عاقبت}
کامنت کن راه‌حل رو بگم!\"\"\"
        ]
        
        topics = [
            'یادگیری پایتون', 'استفاده از ChatGPT', 'کسب درآمد از برنامه‌نویسی',
            'ساخت رزومه حرفه‌ای', 'مصاحبه کاری موفق'
        ]
        
        topic = random.choice(topics)
        template = random.choice(viral_templates)
        
        data = {
            'موضوع': topic,
            'راه‌درست': 'روزانه ۱ ساعت پروژه عملی کار کن',
            'عاقبت': 'از رقبا عقب می‌مونی'
        }
        
        for key, value in data.items():
            template = template.replace('{' + key + '}', value)
        
        return template

# ==================== کلاس بهینه‌ساز کانال ====================
class ChannelOptimizer:
    \"\"\"بهینه‌ساز هوشمند کانال\"\"\"
    
    def suggest_channel_name(self):
        \"\"\"پیشنهاد نام بهینه برای کانال\"\"\"
        names = [
            'هوش مصنوعی | آموزش کاربردی',
            'AI ایران | از صفر تا درآمد',
            'پایتون و هوش مصنوعی | پروژه‌های واقعی',
            'ChatGPT حرفه‌ای | ترفندهای طلایی',
            'برنامه‌نویسی هوشمند | آینده‌سازان'
        ]
        return random.choice(names)
    
    def suggest_description(self):
        \"\"\"پیشنهاد توضیحات بهینه\"\"\"
        descriptions = [
            \"\"\"🎯 آموزش عملی هوش مصنوعی و برنامه‌نویسی
✅ پروژه‌های واقعی با درآمد دلاری
✅ جامعه فعال AI ایران

🔥 هر روز آموزش رایگان
🔔 نوتیفیکیشن رو روشن کن\"\"\",
            
            \"\"\"🧠 مرجع آموزش هوش مصنوعی فارسی
📚 پایتون، ChatGPT، یادگیری ماشین
💼 راه‌های کسب درآمد از تکنولوژی

🎁 منابع رایگان در پین شده\"\"\",
            
            \"\"\"🚀 انقلاب هوش مصنوعی شروع شده!
🤖 آموزش ChatGPT، Midjourney، پایتون
💎 پروژه‌های عملی با درآمد واقعی

✅ همه آموزش‌ها رایگان و پروژه‌محور\"\"\"
        ]
        return random.choice(descriptions)
    
    def generate_hashtags(self, content):
        \"\"\"تولید هشتگ‌های هوشمند\"\"\"
        # هشتگ‌های ثابت
        base_tags = ['#هوش_مصنوعی', '#AI', '#آموزش_رایگان']
        
        # استخراج کلمات کلیدی از محتوا
        words = re.findall(r'[\\w\\u0600-\\u06FF]+', content.lower())
        common_words = Counter(words).most_common(5)
        
        # ساخت هشتگ از کلمات پرتکرار
        content_tags = [f'#{word[0]}' for word in common_words 
                       if len(word[0]) > 2 and word[1] > 1][:3]
        
        # هشتگ‌های موضوعی
        topic_tags = ['#پایتون', '#برنامه_نویسی', '#تکنولوژی', '#ایران', '#کسب_درآمد']
        
        # ترکیب هوشمند
        all_tags = base_tags + content_tags + topic_tags[:2]
        random.shuffle(all_tags)
        
        return ' '.join(all_tags[:8])  # حداکثر ۸ هشتگ

# ==================== اجرای اصلی ====================
def main():
    print('🚀 شروع ربات هوش مصنوعی پیشرفته...')
    
    # ایجاد نمونه‌ها
    analyzer = UltimateAIAnalyzer()
    generator = GeniusContentGenerator(analyzer)
    optimizer = ChannelOptimizer()
    
    # تولید محتوای هوشمند
    print('🧠 در حال تولید محتوای هوشمند...')
    content = generator.generate_smart_content()
    
    # بهینه‌سازی محتوا
    print('🎨 در حال بهینه‌سازی محتوا...')
    hashtags = optimizer.generate_hashtags(content)
    
    # اضافه کردن زمان و هشتگ
    final_content = f'{content}\\n\\n⏰ {datetime.now().strftime(\"%H:%M\")}\\n{hashtags}'
    
    # ارسال به کانال
    print(f'📤 ارسال به کانال {CHANNEL}...')
    
    url = f'https://tapi.bale.ai/bot{TOKEN}/sendMessage'
    payload = {
        'chat_id': CHANNEL,
        'text': final_content,
        'parse_mode': 'HTML'
    }
    
    try:
        response = requests.post(url, json=payload, timeout=30)
        
        if response.status_code == 200:
            print('✅ پست با موفقیت ارسال شد!')
            print(f'📊 محتوا: {content[:50]}...')
            
            # پیشنهاد بهینه‌سازی کانال
            print(f'💡 پیشنهاد نام کانال: {optimizer.suggest_channel_name()}')
            print(f'📝 پیشنهاد توضیحات: {optimizer.suggest_description()[:50]}...')
            
        else:
            print(f'❌ خطا در ارسال: {response.status_code}')
            print(response.text)
            
    except Exception as e:
        print(f'⚠️ خطا: {e}')

# اجرای ربات
if __name__ == '__main__':
    main()
        "
